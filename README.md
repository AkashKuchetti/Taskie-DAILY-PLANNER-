# Taskie-DAILY-PLANNER-
DAILY-PLANNER :This code stores upto 15 tasks, tracks them with count ,and uses a clear menu to add tasks, view one task , view all tasks, or exit.input is handled with getchars() and fgets(),and task numbers are validated.the switch clearly manages all planner functions
